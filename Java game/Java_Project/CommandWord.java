/**
 * enums specific CommandWord with eligible commands
 */
public enum CommandWord {
	BACK, CREATE, DIE, DROP, GO, HELP, INFO, INSPECT,
	INVENTORY, LOAD, MAP, PASS, PICKUP, QUIT, SAVE, UNKNOWN, USE
}
